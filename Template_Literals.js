// get the values when we adding and print the values 


let n=3
let b=5

let res= n+b;
//insted of writing this we write in other way using literals
console.log("the first number is "+ n + " and second number is "+ b + " added sum is "+ "="+ res);

//here we used literals

console.log(`the adding numbers are ${n} and ${b} are gives the sum is ${res}`);

